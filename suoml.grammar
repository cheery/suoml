file:
    (=stmt)*

stmt:
    symbol "(" sep(symbol, ",") ")" >expr / def

expr:
    symbol "=" expr =expr               / let
    "fn" "(" sep(symbol, ",") ")" expr  / abs
    expr >"(" sep(expr, ",") ")"        / app
    term

term:
    "(" expr ")"
    symbol / var
    string / string
    int    / int

terminal symbol, string, int
